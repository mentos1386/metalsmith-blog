.footer-clear
footer
    a(href='#') To The Top
    a(href='mailto:mentos1386@tjo.space') Contact Me

script(async src="/js/tether.js", type="text/javascript")
script(async src="/js/moment.js", type="text/javascript")
script(async src="/js/main.js", type="text/javascript")
script(async src="/js/disqus.js", type="text/javascript")

script.
  const disqus_config = function () {
    this.page.url        = config.domain + '/' + path;
    this.page.title      = title || config.title;
    this.page.identifier = realPath;
  };

  if ( !onLoad ) var onLoad = [];
  window.onload = function () {
    onLoad.forEach(function ( run ) {
      run()
    });
  };